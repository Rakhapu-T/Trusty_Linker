- Set the following serial masks
	  - UART TX GPIO = 5 (I had to resolder to pin-5 -> Not sure if resoldering was absolutely necessary)
	  - UART RX GPIO = 4
	  - UART RTD GPIO = 0
	  - UART CTS GPIO = 0
- Initial setup has baude rate = 921600 (The value can be set in QGC)
- Questions:
	  - Why use RC communication line?
	  - Are you sure we can use Dronebridge as a proper RC replacement?
	  - Worth buying RC transcievers rather than going through the ESP approach (I will largely be simulating first anyway. But this may be useful to measure comm delays.)?
	  - Why not use TX1 and RX1 (Telem_1) which come with flow control?